# Core Flask application
Flask>=3.0.0
Jinja2>=3.0.0
Werkzeug>=3.0.0
flask-cors>=4.0.0

# HTTP requests
requests>=2.31.0

# Environment configuration
python-dotenv>=1.0.0

# Map and navigation
folium>=0.15.0
geopy>=2.4.1

# SSL certificates (for geocoding on macOS)
certifi>=2023.0.0

# Bluetooth Low Energy (cross-platform)
bleak>=0.22.0

# D-Bus Python bindings (for BlueZ native AVRCP media control on Linux)
# Note: Only works on Linux, will fail gracefully on other platforms
dbus-python>=1.3.2

# Sense HAT (Raspberry Pi only - will fail gracefully on other platforms)
# sense-hat>=2.2.0

